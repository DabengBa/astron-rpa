# Standalone（独立脚本导出）说明

本目录用于实现 PRD《导出独立脚本（Standalone Script Export）》相关能力。

阶段一（`docs/specs/251230-standalone-script-export/tasks-stage1.csv`）目标：

- 定义“依赖分析器 / 打包器 / 启动器”的接口与最小实现骨架
- 明确“形态 A 自包含目录”的产物目录契约
- 落地 `manifest.json`（v1）schema 与校验

